# AGENTS.md — 给协作 LLM/Agent 的生产规则

如果你是被要求为本仓库添加内容的 AI agent，先读本文件，再读 `docs/production-guide.md`。

## 硬性规则

1. **不凭记忆写卡片。** 必须基于 `sources/` 中登记的可访问文本，或你确实能打开并核验的公开原文。
2. **无源不录。** 每条卡片必须有 `source` 与可定位的 `ref`；无法定位的内容只能作为“编辑者建议”写在主题页末尾，并明确标注。
3. **原文与转述分离。** quote 卡在正文放原文；中文转述/解释不能冒充原文。
4. **不整书转载。** 单条摘录保持短篇幅，遵守 `docs/copyright-policy.md`。
5. **不把解读写成苏霍姆林斯基原话。** 原创解释要标注为“编辑者建议/应用启示”。
6. **保持双入口一致。** 新增主题/卡片必须同步 `INDEX.md`；`README.md` 与 `SKILL.md` 只做路由，不复制目录。

## 层的划界（2026-09-14 加，接 Karpathy「LLM wiki」方法）

本仓库现在有明确的**层**，AI 只能写其中一层：

| 层 | 路径 | 谁写 | 规矩 |
|---|---|---|---|
| 可引用层 | `cards/`（1386 张） | **只有人** | **AI 只读**。这是出处链的端点，要对外发布、要被人引用——AI 改一个字，出处链就断了 |
| 旁挂层 | `wiki/` | **AI 全权** | 综合、概念页、问答归档、对比页。动手前先读 `wiki/_schema.md` |
| 个人层 | `notes/` | 只有人 | **已 gitignore**（个人批注不进公开仓库，也不进 git 历史） |
| 规格层 | `AGENTS.md` / `taxonomy.md` / `schemas/` | 人机共演进 | 改规格要同步跑 `python scripts/validate_all.py` |

7. **AI 不许改 `cards/`。** 需要补充、勘误、综合，一律写进 `wiki/`，并在页内用 `[[sk-XXXX]]` 指明依据。
   这条是对 Karpathy 原话（"你几乎从不自己写 wiki——LLM 写并维护全部"）的**主动偏离**：
   他假设 wiki 是 LLM 的草稿层；我们的卡片是**可引用层**，所以只有人能改。
8. **卡对卡引用一律写 `[[sk-XXXX]]`。** 不写英文文件名（太长），**更不许写 `[[中文标题]]`**——
   全库有 64 组标题重复（同篇第二张卡），会指向不确定的那一张。
9. **问答要归档，不许消失在聊天里。** 好问题 + 好答案写进 `wiki/queries/<日期>-<slug>.md`，
   并同步 `wiki/index.md` 与 `wiki/_log.md`；页内数字**从生产数据现算**，不手抄。
