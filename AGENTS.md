# AGENTS.md — 给协作 LLM/Agent 的生产规则

如果你是被要求为本仓库添加内容的 AI agent，先读本文件，再读 `docs/production-guide.md`。

## 硬性规则

1. **不凭记忆写卡片。** 必须基于 `sources/` 中登记的可访问文本，或你确实能打开并核验的公开原文。
2. **无源不录。** 每条卡片必须有 `source` 与可定位的 `ref`；无法定位的内容只能作为“编辑者建议”写在主题页末尾，并明确标注。
3. **原文与转述分离。** quote 卡在正文放原文；中文转述/解释不能冒充原文。
4. **不整书转载。** 单条摘录保持短篇幅，遵守 `docs/copyright-policy.md`。
5. **不把解读写成苏霍姆林斯基原话。** 原创解释要标注为“编辑者建议/应用启示”。
6. **保持双入口一致。** 新增主题/卡片必须同步 `INDEX.md`；`README.md` 与 `SKILL.md` 只做路由，不复制目录。
