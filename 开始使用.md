# 开始使用（这一页就是说明书）

> 你现在在 Obsidian 里，打开的是苏霍姆林斯基教育知识库。
> 它同时是：**一个 Obsidian 库**（你自己查、自己写）和一个**公开网站**（对外分享、引用）。

## 一、先记住一件事：三层，各改各的

| 层 | 在哪 | 谁改 | 为什么 |
|---|---|---|---|
| **可引用层** | `cards/` 1386 张 | **只有你，但尽量别改** | 要对外发布、被人引用，是出处链的端点 |
| **旁挂层** | `wiki/` | AI 全权（你也能改） | 综合、概念页、问答归档 |
| **个人层** | `notes/` | **只有你** | 你的想法，**不上传 GitHub** |

**一条最重要的规矩：不要在 `cards/` 里写自己的想法。** 写进 `notes/`。
否则以后"哪句是苏霍姆林斯基、哪句是你"就永远分不清了。

## 二、四个按键（够用 90%）

| 按键 | 干什么 | 说明 |
|---|---|---|
| `Ctrl+O` | **快速打开** | 打**中文标题**或**卡号**都行：`后进生`、`sk-0014`、`A11` |
| `Ctrl+Shift+F` | **全库搜索** | 搜正文，包含每张卡的「教育场景/应用」 |
| 右侧面板 | **反向链接** | 这张卡被哪些笔记/页面提到过——**这是"找得到"的关键** |
| `Ctrl+G` | **关系图** | 看整体结构；点一个点会高亮它的邻居 |

> 右侧面板默认可能没开：`Ctrl+P` → 输入「反向链接」或「Backlinks」即可打开。

## 三、三个日常场景（照着做就行）

### 场景 1：我想找他说过的某个意思

1. `Ctrl+Shift+F`，搜**你会怎么问**的词——「坐不住」「说谎」「家长打骂」「后进生」
   （不要搜"教育"这种大词）
2. 打开命中的卡 → 看**右侧反向链接**，知道还有谁提到它
3. 卡里往下拉，看 **「教育场景 / 应用」**：这一段直接告诉你"什么时候用得上、怎么用"，
   里面的 `sk-XXXX` 可以点开，是互相印证的线索

### 场景 2：我想系统看一个话题

从 `wiki/index.md` 进 →

> **观察角度页**（5 个）→ **条目页**（22 个）→ 把 **「主归属」和「交叉参见」两节合起来读**

为什么必须合起来读：一张卡**只有一个主归属**，但它常常同时讲两件事。
比如讲劳动的卡里，有的主条目其实是「思维与智力」。
（实测：跨角度的参见比同角度内的还多。）

### 场景 3：我想记下自己的想法

1. 在 `notes/` 里新建笔记，**文件名以卡号开头**：`sk-0014 劳动像食物一样.md`
2. 正文里写 `[[sk-0014]]` 引那张卡
3. 以后打开 `sk-0014`，**右侧反向链接里就能看见你的这条笔记**

这样你的想法和卡片是两个文件，但**永远连在一起**。

## 四、会踩的坑（诚实说）

- **`cards/` 改动会跟着网站发布出去**。改了也没关系（`python scripts/validate_all.py` 会检查
  出处与一致性），但别把个人感想写进去。
- **Obsidian 是本地单机的**：手机上看不了。要看手机 → 打开网站
  <https://craeatzeven.github.io/sukhomlinsky-education-kb/>（或自己配 Sync）。
- **这个库有两种命名**：卡片是英文 slug（`sk-0001-backward-children-are-frailest-flowers.md`），
  wiki 层是中文（`A11 劳动与创造.md`）。
  **你不用记文件名**——`Ctrl+O` 打中文或卡号都能跳。
- **首次索引 1386 个文件要一会儿**，关系图会慢慢长出来。

## 五、想再往里走

| 文档 | 讲什么 |
|---|---|
| `wiki/_schema.md` | 旁挂层的规矩（谁写什么、怎么命名、怎么链接） |
| `taxonomy.md` | 分类怎么定的：5 个观察角度 × 23 个条目 + 17 个分面 |
| `AGENTS.md` | AI 在这个仓库里的硬性规则（含层的划界） |
| `docs/kb-status.md` | 全库状态总表：规模、覆盖审计、质量门、判据清单 |
| `wiki/index.md` | 旁挂层的内容目录（5 个角度 + 22 个条目入口） |
