name = "suk-kb-api"
main = "src/index.js"
compatibility_date = "2024-11-01"
workers_dev = true

# 自定义域名（可选）：先在 Cloudflare 里把域名接入，然后解开注释
# routes = [
#   { pattern = "kb-api.example.com", custom_domain = true }
# ]

[vars]
# 允许的前端来源（已收紧到站点域名）
CORS_ORIGIN = "https://craeatzeven.github.io"

[[d1_databases]]
binding = "DB"
database_name = "suk-kb"
database_id = "e8883afd-c7e5-4b9b-bb84-fa3be965a3c8"
