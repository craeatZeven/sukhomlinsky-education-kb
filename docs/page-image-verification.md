# 页图核对记录（定位页码与纸本页码）

> 目的：站上每张卡都说「原文在 第X卷 · 扫描件第 N 页」。这句话必须是真的。
> 方法：用 PyMuPDF 把扫描件那一页渲染成图，**看图**核对（不是拿 OCR 文本自我印证）。
> 复现：`D:\python\python.exe scripts/verify_locator_pages.py --sample`（每卷抽一张），
> 或 `verify_locator_pages.py sk-1392` 单张核。

## 一、2026-09-21 的核对：每卷一页

| 卷 | 扫描件页（1 基） | 卡 | 定位里的小节 | 页上是否有该卡引文 |
|---|---|---|---|---|
| 1 | 277 | sk-0104 | 教师的人格、教师集体与学生的全面发展 | ✅（页上还有「第 19 章」标题、书眉「全面发展的人的培养问题」） |
| 2 | 367 | sk-0093 | 怎样教孩子正确对待脑力劳动 | ✅（书眉「怎样培养真正的人」） |
| 3 | 667 | sk-0116 | 精神素养、道德和无神论 | ✅（书眉「公民的诞生」） |
| 4 | 248 | sk-0094 | 学生的健康与精神生活 | ✅（书眉「苏霍姆林斯基选集（五卷本）」） |
| 5 | 444 | sk-0074 | 要善于表扬好人好事 | ✅ |

**结论：5/5 命中。定位的「卷 + 扫描件页」是对的，小节名与页上书眉/标题一致。**

## 二、由此发现并修掉的一个全站错误：页码少 1

- 语料的 `page` 是 **0 基**：`_ingest.py` 写的是 `page = 块起始 + 块内页号`，而块名 `p0400-0499` 本身 0 基。
- 查证：语料 `page=448` 的那句「教师不爱学生，无异于歌手没有嗓音…」，
  渲染**扫描件 1 基第 449 页**才找到它（页上小节《怎样爱学生》）。
- 而旧定位脚本 `_locate9b.py` 直接取 `unit.page` → **存量 1,259 条同样少 1**。
- 已修：`card-locators.jsonl` 共 1,080 条 +1（新落 193 条同步，全部 1,452 条口径一致，新增 `page_base` 字段）；
  卡片页显示改成 **「扫描件第 N 页」**（`web/card.html`），并在注释里留下这段证据。

## 三、纸本页码：**不发布**，原因如实写在这里

页脚印着**纸本页码**（与扫描件页序不同）。2026-09-21 读到的几个：

| 卷 | 扫描件页 | 页脚印的纸本页（目测） | 差值 |
|---|---|---|---|
| 1 | 277 | 260 | 17 |
| 2 | 367 | 336 | 31 |
| 4 | 248 | 220 | 28 |
| 5 | 444 | 418 | 26 |
| 3 | 667 | 读不出（该卷扫描件分辨率低，页脚是装饰体） | — |

**为什么不把它写进卡片：两条反证。**

1. **差值在卷内不一定恒定。** 卷1 第 300 页的页脚目测读到「300」（该页为偶数页，页码在左侧），与上表的 17 矛盾 —— 说明我这几处目测里至少有一处不可靠，**没有复核过的东西不写进产品**。
2. **不能拿选本的页码反推。** 202 张卡同时有「扫描件页」和「选本标注的纸本页」，
   逐卷求差得到的是散沙：卷3 从 −158 到 +615、卷4 从 −264 到 +865。
   原因是选本引的是**文章起始页**，而引文常在文章深处 —— 两者之差 = 文章长度 + 偏移，不是常数。

所以站上只说 **「扫描件第 N 页（1 基）」**：这个数**每一卷都抽验过**，读者拿同一套扫描件就能翻到。

## 四、要把纸本页码也发布，还差什么

1. 每卷读 **2 个页脚**（奇数页在**右**、偶数页在**左**，裁错边就会读到空白——本轮踩过）标定偏移；
2. 卷3 的扫描件页脚分辨率过低，可能要换一份更清楚的扫描件，或直接在纸本上读；
3. 标定后写 `card-locators.jsonl` 的 `printed_page` 字段，多处读数为同一差值才发布。

## 五、这一轮的产物

- `scripts/verify_locator_pages.py`：按卡号/卷页渲染页图（可复现本轮核对）
- `local_working_copy/fulltext/card-locators.jsonl`：页码口径统一为 1 基
- `web/card.html`：显示「扫描件第 N 页」
- 页图证据：`local_working_copy/page-verify/`（可再生，不入库）

## 六、存疑卡逐张核对（2026-09-21，10 张）

范围：`cards/` 里**我自己标了存疑**的 10 张（7 张「存疑说明」+ 3 张「截断说明」）。
方法同上：渲染该卡所在页（必要时裁高倍看单行、或看相邻页）。**结论有四张是"我标错了"。**

| 卡 | 卷·扫描件页 | 当时标的存疑 | 页图上看到 | 处置 |
|---|---|---|---|---|
| sk-1409 | 4·685 | 开头被截（拼接处缺半句） | 那段**正是**从「教师的最细致…」起的 | 删掉错的截断说明 |
| sk-1410 | 4·38 | 疑缺「启」字 | 页上就是「创设那种能激发道德行为…」 | 删掉错的截断说明 |
| sk-1415 | 4·338 | 句首「苏徂姆林斯」是乱码书眉 | 页上作「我们告诫教育工作者：…」 | **删句首乱码**（原先只标未删） |
| sk-1416 | 5·413 | 「年的教育生涯」缺数字 | 页上作「**30 年**的教育生涯」 | **补「30 年」** |
| sk-1419 | 2·458+459 | 回补的「自己的学生们有」对不对 | 458 页止于「我们应当启迪自己」，459 页起于「的学生们有一股精神力量」 | **回补正确**，改成已核说明 |
| sk-1421 | 1·803+804 | 开头被截 | 803 页尾作「首先，必须深信教育的力量。**这种信心应该成为**」 | 卡文完整无误；删错的截断说明，改标「句中摘引」 |
| sk-1487 | 2·229 | 疑脱「看待」 | 页上作「你的每个学生怎样**看**别人」 | **「着」→「看」**（OCR 形近误读，不是脱字） |
| sk-1568 | 2·816 | 句末「创造生活和关」疑误 | **页上确实印作「创造生活和关」** | 原书如此；卡片照录，说明改为已核 |
| sk-1573 | 3·360 | 句首「情感的劳动」，前缺主语 | 上一行是**小节标题《充满崇高情感的劳动》** | 正文改自「劳动要成为一种大的教育力量」起；补起点说明 |
| sk-1472 | 3·69 | 句末多一个「童话」 | 页上是「…赋了这幅画以生命。**童话，形象地讲，**这是能够吹燃孩子…」 | **删句尾「童话」**（它是下一句的主语；OCR 并了两句）；并补上这条此前缺失的定位 |

**这一轮最值得记的两条：**

1. **四张「我标错了」**：三张是我把「正常段落起头」误判成「开头被截」，一张是我把 OCR 的形近误读（着/看）当成脱字。**存疑标注本身也会错，只有看图能纠正。**
2. **两张是"书上就这样"**：sk-1568 的「创造生活和关」与 sk-1409/1410 的段落起头，页图证明原书如此 —— 这类必须保留，不能"顺"成通顺的句子。

## 七、机械类校勘的抽样复核（2026-09-21）

范围：131 张有 `ocr_fixes` 的卡里，**改动了实义字**（非标点/破折号/顿号）的 101 张。
抽样（固定种子 20260921）抽 3 张看页图，每张只问一件事：**书上是正字，还是 OCR 认错？**

| 卡 | 卷·扫描件页 | 我的改动 | 页上实际印的是 |
|---|---|---|---|
| sk-1469 | 3·167 | 自已 → 自己 | 「我向**自己**提出任务」 ✅ 书上正字 |
| sk-1454 | 4·95 | 教育I作 → 教育工作 | 「学校和教育**工**作的实质所在」 ✅ 书上正字 |
| sk-1417 | 5·414 | -门科学 → 一门科学；细致人微 → 细致入微 | 「不仅是**一**门科学」「细致**入**微」 ✅ 书上正字 |

**结论：这三类的错都在 OCR，不在书。** 改动的方向是对的（`自已/人微/I作/-门` 都是形近字或字母数字混入，构造上只有一种合理读法）。

### 由此纠正的一处**措辞错误**（17 张）

我在约 100 张卡里写过「（选集同错）」——那等于**替纸本背锅，而且是没验证的断言**。
页图抽验 4 例全是书上正字，故 17 张卡里的这类措辞统一改成
**「（OCR 误读；2026-09-21 页图抽验：书上作正字）」**。
（`cards/` 里「同错」二字现已归零。）

**教训**：写「来源错了」和写「我读错了」是两种完全不同的断言，后者只需要我承认，前者需要证据。
